/*Описать структуру НТML-документа, теги и их атрибуты. Семантическая разметка HTML-документа.
Структура HTML- документа строго фиксирована:
•        начинается с <HTML> и заканчивается </HTML>
•        cостоит из раздела заголовков и тела документа
•        раздел заголовков ограничен тегами <HEAD> </HEAD>
•        между ними помещают, например заголовок документа между тегами <TITLE> и </TITLE>
•        сам текст документа располагается в его теле и ограничивается тегами <BODY> и </BODY>
Теги – коды HTML, с помощью которых выполняется разметка текста
•        Тег представляет собой набор символов между угловыми скобками <...>
•        Регистр букв в теге не имеет значения
•        Теги, воздействующие на часть документа бывают открывающие и закрывающие : <…> … </…>
•        Теги, дающие разовый эффект в месте их появления в закрывающей части не нуждаются
•        Если использован тег, отсутствующий в HTML или не поддерживаемый браузером, он игнорируется
Атрибуты тегов
Открывающие теги могут содержать атрибуты, влияющие на эффект, производимый тегом
●	это дополнительные ключевые слова, отделенные от содержимого тэга и друг от друга пробелами
●	значение отделяется от атрибута знаком =
●	значение атрибута должно быть в кавычках, но иногда их можно не писать
Закрывающие теги никогда не имеют атрибутов!
Семантическая разметка HTML — это использование тегов по их прямому назначению (смыслу), а не для внешнего вида. Она заменяет безликие контейнеры <div> на содержательные теги (<header>, <article>, <nav>)
2. Определить объектную модель BOM.
BOM – это объектная модель браузера, которая предоставляет доступ к возможностям браузера и позволяет управлять окном браузера, его параметрами и взаимодействием с пользователем.
BOM представляет браузер в виде набора объектов, главным из которых является объект window – представляет текущее окно веб-обозревателя, а также позволяет изменять его размеры, перемещать его и т. д. Имеет множество свойств и методов. Через него осуществляется доступ к другим объектам модели.
Основные объекты BOM
•	window – главный объект, представляет окно браузера.
•	navigator – содержит информацию о браузере и системе пользователя.
•	location – хранит информацию о текущем URL страницы и позволяет изменять адрес.
•	history – предоставляет доступ к истории посещённых страниц.
•	screen – содержит информацию о параметрах экрана пользователя.*/

class Transport {
    #brand;
    #passengers;
    #power;

    constructor() {
        this.#brand = "";
        this.#passengers = 0;
        this.#power = 0;
    }

    setData(brand, passengers, power) {
        this.#brand = brand;
        this.#passengers = passengers;
        this.#power = power;
    }

    showData() {
        console.log("Марка:", this.#brand);
        console.log("Вместимость пассажиров:", this.#passengers);
        console.log("Мощность:", this.#power);
    }
}

class Automobile extends Transport {
    #doors;

    constructor(brand = "", passengers = 0, power = 0, doors = 4) {
        super();
        this.setData(brand, passengers, power);
        this.#doors = doors;
    }

    inputData() {
        let brand = prompt("Введите марку:");
        let passengers = Number(prompt("Введите вместимость пассажиров:"));
        let power = Number(prompt("Введите мощность:"));
        let doors = Number(prompt("Введите количество дверей:"));

        this.setData(brand, passengers, power);
        this.#doors = doors;
    }

    showData() {
        super.showData();
        console.log("Количество дверей:", this.#doors);
    }
}

let car = new Automobile();

car.inputData();   
car.showData();    
