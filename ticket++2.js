/*1. Назовите виды операторов цикла, приведите синтаксис их записи на языке С++, приведите примеры.
1. Оператор while:
Сначала проверяется условие, если оно истинно (true), выполняется тело цикла.
while (условие) {
    // код, который будет выполняться, пока условие истинно
}
Пример:
int i = 0;
while (i < 5) {
    cout << i << endl;
    i++;
}
2. Оператор do-while:
Тело цикла выполняется один раз, затем проверяется условие. Если условие истинно, цикл повторяется.
do 
{
    // код, который будет выполняться хотя бы один раз
} while (условие);
Пример:
int i = 0;
do
 {
    cout << i << endl;
    i++;
} while (i < 5);

3. Оператор for:
Используется, когда известно количество итераций. Включает инициализацию, условие и изменение счетчика.
for (инициализация; условие; изменение(шаг)) 
{
    // код, который будет выполняться
}
Пример:
for (int i = 0; i < 5; i++) 
{
    cout << i << endl;
}

4. Оператор for-each:
Оператор for-each в C++ — это максимально компактный и безопасный способ перебора всех элементов контейнера или массива без необходимости вручную управлять индексами и следить за границами памяти.
for (объявление_переменной : контейнер) {
    // код, который будет выполняться для каждого элемента контейнера
}
Пример:
vector<int> numbers = {1, 2, 3, 4, 5};
for (int num : numbers) {
    cout << num << " ";
}
2. Перечислите виды массивов в контексте языка С++, запишите синтаксис их объявления. Поясните, в чём состоят особенности инициализации и использования массивов разных видов, приведите примеры.
1.	Статический массив 
Синтаксис объявления: int arr[5];
Особенности: Размер массива должен быть известен на момент компиляции, и он не может быть изменен во время выполнения программы.
int arr[5] = { 1, 2, 3, 4, 5 }; // Инициализация при объявлении
for (int i = 0; i < 5; i++) {
    cout << arr[i] << " "; // Использование элементов массива
}
2.	Динамический массив
Синтаксис объявления: int* arr = new int[5];
Особенности: Размер массива может быть установлен во время выполнения программы с помощью оператора new, и память должна быть освобождена с помощью оператора delete после использования.
    int* arr = new int[5]; // Выделение памяти при выполнении
    for (int i = 0; i < 5; i++) {
        arr[i] = i + 1; // Инициализация элементов массива
        cout << arr[i] << " "; // Использование элементов массива
    }
    delete[] arr; // Освобождение памяти после использования
3.	Вектор
Синтаксис объявления: vector<int> vec = { 1, 2, 3, 4, 5 };
Особенности: Вектор является динамическим массивом, который предоставляет удобные методы для добавления, удаления и изменения элементов. Размер вектора может быть изменен во время выполнения программы.
    vector<int> vec = { 1, 2, 3, 4, 5 }; // Инициализация при объявлении
    vec.push_back(6); // Добавление элемента в конец вектора
    for (int i = 0; i < vec.size(); i++) {
        cout << vec[i] << " "; // Использование элементов вектора
    }
3. Составить программу для расчета заданных выражений. Вводить исходные данные с клавиатуры. Обязательно проверять исключительные ситуации. Значение  = 3,1415926.
 
#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    setlocale(LC_ALL, "ru");

    int x = 0;
    int y = 0;
    int z = 0;
    cout << "введите значение x: ";
    cin >> x;
    cout << "введите значение y: ";
    cin >> y;
    cout << "введите значение z: ";
    cin >> z;

    if (1 + x * fabs(y - tan(z))==0)
    {
        cout << "error";
    }
    else
    {
        double h = ((pow(x, y+1) + exp(y - 1)) / (1 + x * fabs(y - tan(z)))) * (1 + fabs(y - x)) + (pow(fabs(y - x), 2) / 2) - (pow(fabs(y - x), 3) / 3);
        cout << "h=" << h;
    }
}
*/
