/*1.	Оператор цикла for в языке С++. Примеры применения.
Оператор цикла for в C++ — это структура управления, позволяющая многократно выполнять блок кода, когда количество итераций заранее известно.
Синтаксис оператора for:
for (инициализация; условие; изменение) {
    // код, который будет выполняться
}
Пример:
for (int i = 0; i < 5; i++) {
    cout << "Iteration " << i << endl;
}
Цикл for с несколькими переменными:

for (инициализация1, инициализация2; условие; изменение1, изменение2) {
    // код, который будет выполняться
}
Пример:
for (int i = 0, j = 0; i < 3 && j < 3; i++, j++) {
    cout << "i = " << i << ", j = " << j << endl;
}
Цикл for со сложным условием:
for (инициализация; условие1 && условие2; изменение) {
    // код, который будет выполняться
}
Пример:
int x = 5, y = 10;
for (int i = 0; i < x && i < y; i++) {
    cout << i << endl;
}
 Использование оператора continue в цикле for:
for (инициализация; условие; изменение) {
    if (условие_продолжения) {
        continue;
    }
    // код, который будет выполняться
}
Пример:
for (int i = 0; i < 5; i++) {
    if (i == 3) {
        continue;
    }
    cout << i << endl;
}
 Использование оператора break в цикле for:
for (инициализация; условие; изменение) {
    if (условие_выхода) {
        break;
    }
    // код, который будет выполняться
}
Пример:
for (int i = 0; i < 5; i++) {
    if (i == 3) {
        break;
    }
    cout << i << endl;
}
2.	Дайте определение понятиям символьные переменные и строки. Организация, размещение в памяти, процедуры и функции обработки строк. Приведите примеры.
Символьные переменные (char) – представляют символы, такие как буквы, цифры и специальные символы.
Пример: 
     char symbol = 'A'; // Объявление символьной переменной со значением 'A'
Строка (string) – массив символов, заканчивающийся нуль-символом (символ с кодом равным 0; записывается ‘\0’).
Пример: 
       char myString[] = "Hello"; // Объявление строки как массива символов
       #include <string>
       string myString = "Hello"; // Объявление строки с использованием класса string
Символьные переменные обычно занимают один байт памяти, а строки, представленные как массивы символов, занимают столько байтов, сколько символов в строке, плюс один байт для нулевого символа.
Процедуры и функции обработки строк 
Использование встроенных функций для работы со строками, таких как strlen, strcpy, strcat, strcmp и другие. Кроме того, можно использовать операции над строками, предоставляемые классом string, такие как length(), append(), substr() и т.д
3. Составить программу для расчета двух значений z1 и z2, результаты которых должны совпадать. Ввод исходных данных можно задавать при декларации или вводить с клавиатуры. Игнорировать возможность деления на ноль. Значение  = 3,1415926.
 
#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    setlocale(LC_ALL, "ru");

    int b = 0;
    cout << "введите значение b: ";
    cin >> b;

    if ((b*b - 4) + b + 2 <= 0 || b + 2 <= 0)    
{
        cout << "error";
    }
    else
    {
        double z1 = sqrt(2 * b + 2 * sqrt(b * b - 4)) / (sqrt(b * b - 4) + b + 2);
        double z2 = 1 / sqrt(b + 2);

        cout << "z1=" << z1 << endl;
    cout << "z2=" << z2;
    }
}
*/