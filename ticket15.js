/*Определить управление положением на странице (свойства left, top, z-index, position, visibility, overflow). (13)
•	Свойство position – позволяет задать новое местоположение блока относительно того места, где он находился бы в нормальном потоке документа и алгоритм его положения.
Значения свойства position:
o	static – элементы отображаются как обычно (по умолчанию);
o	relative – положение элемента устанавливается относительно его исходного места. Добавление свойств left, top, right и bottom изменяет позицию элемента и сдвигает его относительно первоначального расположения. Позволяет задавать свойство z-index для элемента;
o	absolute – абсолютное позиционирование элемента, при этом другие элементы отображаются на веб-странице так, словно абсолютно позиционированного элемента нет. Положение элемента задается свойствами left, top, right и bottom.
o	fixed – фиксированное позиционирование относительно окна браузера и не меняет положения при прокрутке веб-документа;
o	sticky – переключается между relative и fixed. Элемент ведет себя как обычно, пока не достигнет определенной точки при скролле, после чего "залипает”.
•	Свойства top и left – используются для смещения позиционированного элемента от краев содержащего блока.
Работают, если position не static.
o	top – смещение от верхней границы (bottom - нижней);
o	left – смещение от левого края (right - правого).
•	Свойство z-index
Определяет порядок наложения позиционированных элементов по оси Z (глубина). Чем выше число, тем "ближе" к пользователю находится элемент.
•	Свойство visibility
Управляет видимостью элемента, не удаляя его из потока:
o	visible – элемент отображается;
o	hidden – элемент скрыт, но место под него сохраняется.
•	Свойство overflow
Управляет отображением содержимого, выходящего за границы элемента:
o	visible – содержимое отображается полностью;
o	hidden – лишнее скрывается;
o	scroll – добавляются полосы прокрутки;
o	auto – прокрутка появляется при необходимости.
Описать валидацию форм, регулярные выражения в JavaScript.
Обработка форм на стороне клиента – позволяет проверять данные формы в браузере ДО отправки на сервер с мгновенной обратной связью для пользователя.
Цель валидации – предотвратить ошибки ввода и дать мгновенную обратную связь пользователю.
Проверяемые условия:
	пустые поля;
	корректность email;
	допустимая длина строки;
	соответствие определённым правилам (например, пароль с цифрами и буквами).
Регулярные выражения – мощный инструмент для проверки формата строк, например, электронной почты, телефона или пароля.
var pattern = /^[a-zA-Z0-9]+$/; // только буквы и цифры
Методы для проверки строки
•	pattern.test(string) – возвращает true, если строка соответствует шаблону.
•	string.match(pattern) – возвращает совпадения с шаблоном. */
class City {
    #name;
    #population;
    #universities;

    constructor() {
        this.#name = "";
        this.#population = 0;
        this.#universities = 0;
    }

    setData(name, population, universities) {
        this.#name = name;
        this.#population = population;
        this.#universities = universities;
    }

    showData() {
        console.log("Название города:", this.#name);
        console.log("Количество жителей:", this.#population);
        console.log("Количество ВУЗов:", this.#universities);
    }
}

class School extends City {
    #graduates;

    constructor(name = "", population = 0, universities = 0, graduates = 0) {
        super();
        this.setData(name, population, universities);
        this.#graduates = graduates;
    }

    inputData() {
        let name = prompt("Введите название города:");
        let population = Number(prompt("Введите количество жителей:"));
        let universities = Number(prompt("Введите количество ВУЗов:"));
        let graduates = Number(prompt("Введите количество выпускников:"));

        this.setData(name, population, universities);
        this.#graduates = graduates;
    }

    showData() {
        super.showData();
        console.log("Количество выпускников:", this.#graduates);
    }
}

let school = new School();
school.inputData();
school.showData();
